/** Shared slider card slot dimensions (content + music). */
export const SLOT_W = 160;
export const SLOT_H = 290;

/** Fixed collection card height — used globally for consistent grid rows. */
export const COLLECTION_CARD_H = 268;
